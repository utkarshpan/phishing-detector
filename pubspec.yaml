name: phishing_detector
description: "AI-Powered Phishing Detector"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  url_launcher: ^6.1.0
  shared_preferences: ^2.2.0
  flutter_native_splash: ^2.3.0
  google_fonts: ^6.1.0
  dio: ^5.3.0  # ← Moved here - For HTTP requests (URL expansion)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true